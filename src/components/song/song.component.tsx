import React, { useEffect } from 'react';

const SongComponent = () => {
    useEffect(() => {
        return() => {

        };
    }, []);
    return(
        <h1>Musique</h1>
    );
};

export default SongComponent; 